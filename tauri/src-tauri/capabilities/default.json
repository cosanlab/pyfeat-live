{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for Py-feat Live. The Rust shell drives all subprocess spawning directly via std::process — no JS-callable shell APIs are exposed.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default"
  ]
}
