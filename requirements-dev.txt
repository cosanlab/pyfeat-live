-r requirements.txt
watchdog
seaborn
snakeviz
black==24.4.2
httpx>=0.27
pytest-asyncio>=0.24
pytest-timeout>=2.3

# Generate tab needs torch>=2.12 (MPS compile_shader/metal kernel) and the FaceEditor package:
#   uv pip install --python .venv/bin/python "torch==2.12.1" torchvision torchcodec kornia
#   uv pip install --python .venv/bin/python -e /Users/lukechang/Github/au_face_generation
#   uv pip install --python .venv/bin/python --no-deps -e /Users/lukechang/Github/py-feat   # local py-feat with MPS #368
