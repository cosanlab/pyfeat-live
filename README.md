# Py-feat live

This is a quick demo of using py-feat to analyze webcam frames in real time and display the results using streamlit

1. Create a new `conda` or `venv`
2. `pip install -r requirements.txt`
3. `streamlit run app.py`
4. Go to ` http://localhost:8501` in your browser